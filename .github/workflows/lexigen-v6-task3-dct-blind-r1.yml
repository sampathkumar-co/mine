name: LEXIGEN v6 Task 3 DCT-I Official Blind R1 (Archived)
on:
  workflow_dispatch:
permissions: {contents: read}
jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - name: Blind boundary already consumed
        run: |
          echo "Task 3 official blind R1 is consumed by run 32880195492."
          echo "Do not rerun blind evaluation; this workflow is retained only as provenance."
          exit 1
