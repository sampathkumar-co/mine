name: Mini-ORIGIN External Local Quotient v0.41

on:
  push:
    paths:
      - "mini-origin/src/mini_origin/external_local_quotient_v41.py"
      - "mini-origin/tests/test_external_local_quotient_v41.py"
      - "mini-origin/external-data/uci-v41/manifest.json"
      - ".github/workflows/mini-origin-v41-external-local-quotient.yml"
  pull_request:
    paths:
      - "mini-origin/src/mini_origin/external_local_quotient_v41.py"
      - "mini-origin/tests/test_external_local_quotient_v41.py"
      - "mini-origin/external-data/uci-v41/manifest.json"
      - ".github/workflows/mini-origin-v41-external-local-quotient.yml"
  workflow_dispatch:

concurrency:
  group: mini-origin-v41-external-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  external-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    defaults:
      run:
        working-directory: mini-origin
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: mini-origin/pyproject.toml
      - run: python -m pip install -e ".[dev]"
      - name: Verify frozen protocol and parsers
        run: pytest -q tests/test_external_local_quotient_v41.py
      - name: Evaluate five untouched row permutations
        run: >-
          mkdir -p results &&
          python -m mini_origin.external_local_quotient_v41
          --output results/v41-external.json
      - uses: actions/upload-artifact@v4
        with:
          name: mini-origin-v41-external
          path: mini-origin/results/v41-external.json
          retention-days: 90
