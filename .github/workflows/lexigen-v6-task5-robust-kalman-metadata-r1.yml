name: LEXIGEN v6 Task 5 Robust Kalman Dataset Metadata R1 ARCHIVED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Metadata R1 was consumed exactly once as run 32885758041 and is permanently archived."
