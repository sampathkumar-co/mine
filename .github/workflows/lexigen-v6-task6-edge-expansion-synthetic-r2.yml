name: LEXIGEN v6 Task 6 Edge Expansion Synthetic R2 ARCHIVED
on:
  workflow_dispatch:
    inputs:
      archived:
        description: "Task 6 sealed negative before training; do not execute"
        required: true
        default: "archived"
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Task 6 sealed in TASK_RESULT.json; no candidate rescue or official-data access allowed"
