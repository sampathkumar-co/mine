name: LEXIGEN v6 Task 5 Robust Kalman Source Screen R1 ARCHIVED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Source-screen R1 was consumed exactly once as run 32883779264 and is permanently archived."
