name: LEXIGEN v6 Task 6 Edge Expansion Synthetic R1 ARCHIVED
on:
  workflow_dispatch:
    inputs:
      archived:
        description: "R1 exposed directed NetworkX semantic mismatch before official data; do not execute"
        required: true
        default: "archived"
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Synthetic R1 archived; see SYNTHETIC_R1_SCIENTIFIC_FAILURE.json"
