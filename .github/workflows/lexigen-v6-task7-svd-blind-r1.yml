name: LEXIGEN v6 Task 7 SVD Blind R1 [ARCHIVED]
on:
  push:
    branches: [__lexigen_archived_never__]
permissions: {contents: read}
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Archived after successful one-shot blind run 32938958488; do not rerun.'
