name: LEXIGEN World Covering v2 Synthetic Validation

on:
  pull_request:
    branches: [main]
    paths:
      - "lexigen-world-covering-v2-validation/TRIGGER_ONCE"

permissions:
  contents: read

jobs:
  validate-frozen-v2:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: lexigen-world-covering-v2/requirements.txt

      - name: Install frozen dependency
        run: python -m pip install -r lexigen-world-covering-v2/requirements.txt

      - name: Run snapshot-free synthetic validation
        run: python -u lexigen-world-covering-v2-validation/test_synthetic.py

      - name: Upload validation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lexigen-world-covering-v2-synthetic-validation
          path: lexigen-world-covering-v2-validation/VALIDATION_REPORT.json
          if-no-files-found: error
          retention-days: 90
