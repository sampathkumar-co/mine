name: LEXIGEN v6 Task 5 Robust Kalman Official Blind R1 ARCHIVED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Scientific blind R1 was consumed exactly once as run 32886731942 and is permanently archived."
