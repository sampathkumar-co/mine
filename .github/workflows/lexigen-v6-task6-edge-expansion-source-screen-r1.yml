name: LEXIGEN v6 Task 6 Edge Expansion Source Screen R1 ARCHIVED
on:
  workflow_dispatch:
    inputs:
      archived:
        description: "R1 failed before scientific output; do not execute"
        required: true
        default: "archived"
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "R1 archived after infrastructure-only serialization failure; see SOURCE_SCREEN_R1_INFRA_FAILURE.json"
