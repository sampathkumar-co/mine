name: LEXIGEN v6 Task 4 Lorenz96 Official Training R2 [ARCHIVED]
on:
  workflow_dispatch:
    inputs:
      archived:
        description: "Archived evidence workflow; do not rerun"
        required: true
        default: "NO-RERUN"
permissions: {contents: read}
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Task 4 training R2 is permanently archived; canonical run 32882635396."
