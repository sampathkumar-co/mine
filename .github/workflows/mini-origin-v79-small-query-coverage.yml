name: Mini-ORIGIN Small-Query Coverage v0.79

on:
  push:
    branches:
      - research/mini-origin-v79-small-query-coverage
    paths:
      - "mini-origin/campaigns/v79-small-query-coverage.json"
      - "mini-origin/src/mini_origin/small_query_coverage_v79.py"
      - "mini-origin/tests/test_small_query_coverage_v79.py"
      - "mini-origin/scripts/run_v79_ci.sh"
      - ".github/workflows/mini-origin-v79-small-query-coverage.yml"
  workflow_dispatch:

concurrency:
  group: mini-origin-v79-small-query-coverage
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  opened-data-development:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    defaults:
      run:
        working-directory: mini-origin
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: mini-origin/pyproject.toml
      - name: Install Mini-ORIGIN
        run: python -m pip install -e ".[dev]"
      - name: Run opened-data small-query campaign
        run: bash scripts/run_v79_ci.sh
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mini-origin-v79-small-query-coverage
          path: |
            mini-origin/results/v79/python-reference.json
            mini-origin/results/v79/rust-results.json
            mini-origin/results/v79/evidence.json
          if-no-files-found: warn
          retention-days: 90
