name: LEXIGEN v6 Task 3 DCT-I Official Training R1 (Archived)
on:
  workflow_dispatch:
permissions: {contents: read}
jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - name: Training boundary already consumed
        run: |
          echo "Task 3 official training R1 is consumed by run 32879541182."
          echo "Do not rerun training; this workflow is retained only as provenance."
          exit 1
