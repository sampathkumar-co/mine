# Lexigen World Covering v2 — Corrected Synthetic Validation

This isolated branch validates corrected v2 head `d652f4be60fc498d07f478a663fd9ca9c4a70428` without downloading the covering snapshot or revealing real research targets.

It independently checks the exact frozen v1 selector, proves the reserved and v2 synthetic slices are disjoint, verifies every locked Git blob, exercises a known Fano-plane success and an impossible six-block failure, and tests deterministic evidence attribution.

This branch is validation evidence only. It must not be merged and is not a mathematical discovery claim.
