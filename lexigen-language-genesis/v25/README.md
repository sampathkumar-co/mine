# Lexigen Language Genesis v25 ? typed semantic enumeration

v25 replaces fixed mechanism schemas with bottom-up typed program enumeration. Expressions are deduplicated by their complete behaviour across six demonstrations, allowing depth-5 compositions while keeping a deterministic finite budget. Exact programs are colour-abstracted and must recur on at least two discovery identities before held-out validation is opened.
