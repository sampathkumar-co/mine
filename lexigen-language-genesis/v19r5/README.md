# Lexigen Language Genesis v19r5 ? registry factorization

v19r5 redesigns the failed v20 monolithic transfer path. It fixes a deterministic split of every eligible ARC-GEN task identity before generation: 631 discovery tasks and 244 held-out validation tasks.

The complete v19r3 finite meta-grammar is scanned only on discovery identities. Exact descriptors are factorized by structure after removing colour arguments. A structure enters the frozen library only when it independently solves at least two discovery task identities.

Validation generators and outputs must remain unopened until the library is remotely committed. Validation may infer colour arguments only; no action, set-mode, base-mode, or code edits are allowed.

This is public development transfer evidence, not sealed external evidence or a world-level breakthrough.

## Preserved outcome

This revision did not complete discovery. The original scanner and an independently factorized reproducer both found zero exact programs through the 550-task checkpoint, then reached deterministic generator nontermination at discovery identity `e74e1818` (index 573). An isolated diagnostic reproduced the same boundary.

No library was created, held-out validation was not started, and no validation output was opened. The unexecuted validation draft is preserved under `cancelled/`. A new revision must precommit deterministic generation timeouts before scanning again.
