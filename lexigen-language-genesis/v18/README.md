# Lexigen Language Genesis v18 - sealed constructive gate

v18 freezes the exact v17 engine at `cd89382e38b45d12916e662af052a7aa1a374896` before attempting ten untouched sealed gates.

The selected gates are historical v13 campaign gates 4-13. Their demonstrations and test inputs are visible in committed redacted packages; test outputs remain outside Git in the preserved non-Git vault. Gate identities cannot be replaced.

For each gate, v18 performs one deterministic frozen-v17 synthesis attempt. A generated program, verifier contract and all test predictions must be remotely committed before hidden scoring. A no-program result is preserved permanently and is not scored. No retry is allowed after scoring.

v18 is an external capability gate, not a claim of autonomous semantic-substrate invention or a world-level breakthrough.
