# Lexigen v30 task 9565186b fresh validation

This isolated branch freezes the minimum exact v30 candidate for fresh identity `9565186b` before any fresh-case access.

The selected four-node program preserves the inferred background and recolors every non-background cell to color `5`.

The frozen gate uses case indices 0-999, the `lexigen-v30-fresh` seed namespace, the original AST runtime, an independent direct runtime, and a relational verifier derived from the frozen AST.

A pass is evidence on one public held-out task only. It is not a world-level breakthrough.
